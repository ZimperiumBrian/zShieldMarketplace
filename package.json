{
  "name": "zshield-pro-action",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",

  "scripts": {
    "clean": "rm -rf dist",
    "prebuild": "npm run clean",
    "build": "ncc build src/action.js -o dist",
    "prepare": "npm run build"
  },

  "dependencies": {
    "@actions/core": "^1.10.1",
    "axios": "^1.6.8",
    "form-data": "^4.0.0",
    "glob": "^10.3.12"
  },

  "devDependencies": {
    "@vercel/ncc": "^0.38.1"
  }
}
